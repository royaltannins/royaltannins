import React from 'react';
import './Events.scss';

const Events : React.FC = props => {
    return (
        <div className={`events`}>
            Events &amp; Merch à venir… Stay tuned!
        </div>
    );
}

export default Events;